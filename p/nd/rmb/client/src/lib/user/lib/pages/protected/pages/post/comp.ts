import { Component } from '@angular/core';
export const ROOT_SELECTOR = 'post';
@Component({
    selector: ROOT_SELECTOR,
    templateUrl: 'view.html',
})
export class PostComp{
    constructor(
    ) {
    }
}