import { Component, OnInit, ViewEncapsulation } from '@angular/core';
export const ROOT_SELECTOR = 'profile';
@Component({
    selector: ROOT_SELECTOR,
    templateUrl: 'view.html',
})
export class ProfileComp{
    constructor(
    ) {
    }
}