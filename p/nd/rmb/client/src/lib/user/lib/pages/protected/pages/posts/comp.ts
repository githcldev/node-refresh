import { Component } from '@angular/core';
export const ROOT_SELECTOR = 'posts';
@Component({
    selector: ROOT_SELECTOR,
    templateUrl: 'view.html',
})
export class PostsComp{
    constructor(
    ) {
    }
}