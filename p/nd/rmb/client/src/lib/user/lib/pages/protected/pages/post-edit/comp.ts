import { Component } from '@angular/core';
export const ROOT_SELECTOR = 'post-edit';
@Component({
    selector: ROOT_SELECTOR,
    templateUrl: 'view.html',
})
export class PostEditComp{
    constructor(
    ) {
    }
}